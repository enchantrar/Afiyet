# Afiyet — the app

Expo React Native recipe + meal-planning app.

```bash
npm install
npx expo start
```

Requires Node 18+. Node is not preinstalled in this workspace — grab it from
https://nodejs.org and re-run the commands above.

See the [root README](../README.md) for the full feature list.
